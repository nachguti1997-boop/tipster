# Environment variables for Prisma CLI
# This is required because Prisma CLI runs from this directory and needs to know where the DB is.
# You can also use a tool like dotenv-cli to load from apps/api/.env, but having this here is safer for direct prisma commands.

DATABASE_URL="postgresql://user:password@localhost:5432/tipster?schema=public"
